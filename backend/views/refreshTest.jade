extends layout

block content
	ul
	each item in results
		li
			a(href=item.url) #{item.url}
			p #{item.rawText}
			br
			p #{item.firstLine}
			ul
			p #{item.company}
			each tag in item.tags
				span #{tag}, 
			br
			br
			br
